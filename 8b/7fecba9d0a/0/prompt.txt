On the TDD point: you're right, and the honest version is that the red I ran was after the fact and covers bug 1 (code=true) directly; bug 2 (state format) is enforced by tests that couldn't compile against the old struct, so its red is "the browser said so", not a failing unit test. Next time: test first, or say I'm not - lets add guidance to AGENTS.md its linked to claude.md. while your there add a concise note on radar mcp and executor path for netdata metrics